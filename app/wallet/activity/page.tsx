import { ComingSoon } from "../components/coming-soon";

export default function ActivityPage() {
  return (
    <ComingSoon
      title="Activity"
      blurb="Transaction history across all chains. Wired via CoinVoyage order polling once real execution is enabled."
    />
  );
}
