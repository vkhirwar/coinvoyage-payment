import ThemeBuilder from "@/components/ThemeBuilder";

export const metadata = {
  title: "Theme Builder | CoinVoyage PayKit",
  description: "Customize your PayKit widget theme in real-time",
};

export default function ThemeBuilderPage() {
  return <ThemeBuilder />;
}
